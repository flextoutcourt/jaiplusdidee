
/**
 * Take a shift
 * 
 * @param {number} id Id of the agent who takes shift
 */
export async function takeShift(id: number){
    console.log(id);
    // send a pusher notification for all the agents in the same group
}

/**
 * Leave a shift
 * 
 * @param {number} id Id of the agent who leaves shift
*/
export async function leaveShift(id: number){
    console.log(id);
    // send a pusher notification for all the agents in the same group
}